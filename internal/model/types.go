package model

import (
	"github.com/prometheus/common/model"
)

// Re-export the LabelSet type from Prometheus
type LabelSet = model.LabelSet

// Re-export the LabelName type from Prometheus
type LabelName = model.LabelName

// Re-export the LabelValue type from Prometheus
type LabelValue = model.LabelValue 